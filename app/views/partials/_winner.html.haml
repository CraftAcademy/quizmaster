.results
  - data[:list].reverse.each do |team|
    %p #{team[:team].name}: #{team[:score]}
= button_tag 'See Answers', id: 'see_answers', class: 'btn btn-primary btn-lg btn-block'
.results{id: 'answers'}
  - data[:quiz].questions.each_with_index do |question, index|
    %span{style: "font-weight: bold"}
      = "#{index.to_i + 1}."
      = question.body
    %br/
    Answer:
    = question.answer
    %hr/

:javascript
  $('#answers').hide();
  $(document).on('click', '#see_answers', function(e)
    {
      $('#answers').show();
      $('#see_answers').hide();
    });
