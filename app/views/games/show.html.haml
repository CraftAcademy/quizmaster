%h3= message
%p Find me in app/views/games/show.html.haml
- if cookies['team_id'].blank?
  = form_tag '#', id: 'create_team', remote: true do
    = text_field_tag :name
    = submit_tag 'Create Team'
- else
  %p Welcome back team